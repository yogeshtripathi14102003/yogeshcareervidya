export const metadata = {
  title: "New Year Offer",
  description: "Limited-time New Year offers on online degree admissions with CareerVidya.",
  alternates: { canonical: "/new-year" },
};

export default function NewYearLayout({ children }) {
  return children;
}
