export const metadata = {
  title: "Apply Now",
  description: "Apply for open positions at CareerVidya. Submit your application and join our team.",
  alternates: { canonical: "/applynow" },
};

export default function ApplyNowLayout({ children }) {
  return children;
}
